module.exports = {
  // Disable New Architecture for compatibility with all libraries
  project: {
    ios: {
      automaticPodsInstallation: true,
    },
    android: {
      // Android settings if needed
    },
  },
};
